{
  "name": "infinite-llm-dialogue-back",
  "version": "1.0.4",
  "description": "AWS Lambda function for generating responses using Google Generative AI",
  "main": "index.js",
  "scripts": {
    "bump": "npm version patch --no-git-tag-version",
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "zip -r deployment-package.zip index.js package.json node_modules",
    "upload": "aws lambda update-function-code --function-name CallAIAPI --region us-east-1 --zip-file fileb://deployment-package.zip",
    "deploy": "npm run bump && npm run zip && npm run upload"
  },
  "keywords": [],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "aws-sdk": "^2.1692.0",
    "axios": "^1.7.9",
    "dotenv": "^16.4.7",
    "uuid": "^11.1.0"
  }
}
